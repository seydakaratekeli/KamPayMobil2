<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="KamPay.Views.LoginPage"
    Title="KamPay - Giriş">
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="12">

            <!-- 💡 DEMO KULLANICI BİLGİSİ -->
            <Frame Padding="15"
                   CornerRadius="10"
                   BorderColor="#E0E0E0"
                   BackgroundColor="#F5F5F5">
                <VerticalStackLayout Spacing="5">
                    <Label Text="💡 Demo Kullanıcı Bilgileri"
                           FontAttributes="Bold"
                           TextColor="#333"
                           FontSize="14" />
                    <Label Text="E-posta: test@bartin.edu.tr"
                           FontSize="13"
                           TextColor="#555" />
                    <Label Text="Şifre: Test1234"
                           FontSize="13"
                           TextColor="#555" />
                </VerticalStackLayout>
            </Frame>

            <Label Text="Giriş Yap" FontSize="24" HorizontalOptions="Center" />

            <Entry Placeholder="E-posta" Text="{Binding Email}" Keyboard="Email" />
            <Entry Placeholder="Şifre" Text="{Binding Password}" IsPassword="True" />

            <HorizontalStackLayout VerticalOptions="Center">
                <Switch IsToggled="{Binding RememberMe}" />
                <Label Text="Beni hatırla" VerticalOptions="Center" />
            </HorizontalStackLayout>

            <Label Text="{Binding ErrorMessage}" IsVisible="{Binding ErrorMessage}" TextColor="Red" />

            <Button Text="Giriş Yap" Command="{Binding LoginCommand}" />
            <ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}" />

            <Button Text="Hesabın yok mu? Kayıt ol" Command="{Binding GoToRegisterCommand}" BackgroundColor="Transparent" TextColor="Blue" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
